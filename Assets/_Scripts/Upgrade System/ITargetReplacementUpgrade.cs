using UnityEngine;

public interface ITargetReplacementUpgrade
{
    // this is for any upgrades that will completely replace the targeting behaviour

    void Target(BlockController blockController);

}
